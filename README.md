# Unbound Pokedex

A TanStack Start app for browsing and searching Pokemon data from `public/pokemon_data.json`, with live type data and sprites sourced from PokemonDB. Pokemon Data sourced from Pokemon Unbound .c files.

## Development

```bash
npm install
npm run dev
```

App runs on `http://localhost:3000`
